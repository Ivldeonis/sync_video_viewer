# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE are ignored when building from the
# command line, check build.gradle files instead.

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details at
# https://developer.gradle.org/guides/performance/configuration_on_demand/
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are being used
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and does not include transitive dependencies.
android.nonTransitiveRClass=true

# Kotlin Compose compiler
kotlin.jvm.target=17

# Gradle daemon
org.gradle.jvmargs=-Xmx2048m

# Build properties
android.enableJetifier=true
android.enableResourceOptimizations=true
